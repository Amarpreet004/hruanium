# robots.txt for hruanium.com

# robots.txt for hruanium.com

# Allow all crawlers but block internal/system paths
User-agent: *
Disallow: /api/
Disallow: /.next/
Disallow: /private/

# Sitemap location (keep this accurate)
Sitemap: https://hruanium.com/sitemap.xml

# Notes:
# - We intentionally allow URLs with query-strings (no Disallow: /*?*),
#   so search engines can index pages that rely on query parameters.
# - Remove or adjust Disallow entries only for internal or sensitive routes.
